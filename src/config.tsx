let config = {
    restAPI: process.env.REST_API,
    appID: process.env.APP_ID,
    scope: "public_profile,email,user_photos,user_posts,user_friends,manage_pages,publish_pages,pages_show_list"
}

export default config;